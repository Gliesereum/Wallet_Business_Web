import React from 'react';
import bem from 'bem-join';

const b = bem('workerInfoDrawer');

const WorkerInfoDrawer = () => (
  <div className={b()}>WorkerInfoDrawer</div>
);

export default WorkerInfoDrawer;
